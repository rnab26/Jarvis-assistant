<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

    <!-- Fenêtre de l'assistant déclenché par l'appui long : translucide,
         sans titre, sans barre d'action — pas l'app entière qui s'ouvre.
         La taille et l'ancrage en bas se posent en code (WindowManager,
         AssistOverlayActivity) plutôt qu'ici : un thème seul ne peut pas
         dire "un tiers d'écran en bas".
         Parti de Theme.AppCompat.Translucent.NoTitleBar (nom de thème
         plateforme, absent d'AppCompat, rejeté par CI en "resource not
         found") : la translucidité se pose ici par les items
         windowIsTranslucent / windowBackground sur une base NoActionBar
         ordinaire, pas par un parent au nom "Translucent". -->
    <style name="Theme.Jarvis.Assist" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>
</resources>