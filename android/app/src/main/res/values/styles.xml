<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

    <!-- Fenêtre de l'assistant déclenché par l'appui long : translucide,
         sans titre, sans barre d'action — pas l'app entière qui s'ouvre.
         La taille et l'ancrage en bas se posent en code (WindowManager,
         AssistOverlayActivity) plutôt qu'ici : un thème seul ne peut pas
         dire "un tiers d'écran en bas". -->
    <!-- Parent : Theme.AppCompat.NoActionBar.

         « Theme.AppCompat.Translucent.NoTitleBar » n'existe pas — AppCompat ne
         fournit pas de variante translucide. Le lien d'AAPT échouait donc et
         AUCUNE APK ne se construisait plus (« resource style/... not found »),
         ce qui bloquait toutes les mises à jour du téléphone, pas seulement
         l'assistant. Constaté sur la CI le 4 sept. 2026.

         AssistOverlayActivity étend BridgeActivity, donc AppCompatActivity :
         le parent DOIT rester un thème AppCompat, on ne peut pas basculer sur
         android:Theme.Translucent.NoTitleBar. Et ce n'est pas nécessaire — la
         translucidité vient des attributs ci-dessous, que ce style pose
         lui-même. -->
    <style name="Theme.Jarvis.Assist" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>
</resources>